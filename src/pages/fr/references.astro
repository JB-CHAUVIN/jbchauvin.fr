---
import Layout from '../../components/layout/Layout.astro';
import SectionTitle from '../../components/ui/SectionTitle.astro';
import ReferenceCard from '../../components/references/ReferenceCard.astro';
import { useTranslations } from '../../i18n/index';
import referencesData from '../../../data/references.json';

const lang = 'fr';
const t = useTranslations(lang);
---

<Layout
  title="Références — Jean-Baptiste Chauvin"
  description="Références professionnelles et témoignages des entreprises avec lesquelles Jean-Baptiste Chauvin a travaillé."
>
  <div class="p-4">
    <SectionTitle subtitle={t('references.subtitle')}>
      &#128172; {t('references.title')}
    </SectionTitle>

    {referencesData.references.map((ref) => (
      <ReferenceCard reference={ref} lang={lang} />
    ))}
  </div>
</Layout>
